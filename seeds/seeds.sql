CREATE TABLE twentytwenty (
id INT AUTO_INCREMENT NOT NULL,
`rank` INT,
company_name VARCHAR(100),
number_of_employees INT,
change_in_rank INT,
revenues_in_millions INT,
revenue_percent_change DECIMAL(7, 2),
profits_in_millions INT,
profit_percent_change DECIMAL(7, 2),
assets_in_millions INT,
market_value_in_millions INT,
PRIMARY KEY (id)
);

CREATE TABLE twentytwentyone (
id INT AUTO_INCREMENT NOT NULL,
`rank` INT,
company_name VARCHAR(100),
number_of_employees INT,
change_in_rank INT,
revenues_in_millions INT,
revenue_percent_change DECIMAL(7, 2),
profits_in_millions INT,
profit_percent_change DECIMAL(7, 2),
assets_in_millions INT,
market_value_in_millions INT,
PRIMARY KEY (id)
);

CREATE TABLE twentytwentytwo (
id INT AUTO_INCREMENT NOT NULL,
`rank` INT,
company_name VARCHAR(100),
number_of_employees INT,
change_in_rank INT,
revenues_in_millions INT,
revenue_percent_change DECIMAL(7, 2),
profits_in_millions INT,
profit_percent_change DECIMAL(7, 2),
assets_in_millions INT,
market_value_in_millions INT,
PRIMARY KEY (id)
);